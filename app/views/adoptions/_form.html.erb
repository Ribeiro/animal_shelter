<%= form_for(@adoption) do |f| %>
  <% if @adoption.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adoption.errors.count, "error") %> prohibited this adoption from being saved:</h2>

      <ul>
      <% @adoption.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% @current_animal = @adoption.animal %>
  <%= f.hidden_field :animal_id, value: @current_animal.id %>

  <div class="field">
    <%= f.label "Adotante" %><br>
    <%= f.collection_select :adopter_id, Adopter.all, :id, :name %>
  </div>

 <strong>Foto:</strong>
  <%= link_to image_tag(@current_animal.image.url, class: 'media-object img-thumbnail'), @current_animal.image.url, target: '_blank' %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
